/**
 * Base Service class
 * All services should extend this for consistency
 */
export abstract class BaseService {
  // Common service methods can be added here
  // For now, this serves as a marker class
}

